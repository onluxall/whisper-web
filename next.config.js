/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/whisper-web',
  images: {
    unoptimized: true,
  },
  /* config options here */
};

module.exports = nextConfig; 